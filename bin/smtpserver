#!/usr/bin/env ruby
require './mail_server'

EM.run do
  # Hit Control + C to stop
  Signal.trap('INT')  { EM.stop }
  Signal.trap('TERM') { EM.stop }

  Mailbooth::MailServer.parms = { verbose: true }
  Mailbooth::MailServer.start
end
